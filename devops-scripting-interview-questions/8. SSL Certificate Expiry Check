#!/bin/bash

DOMAIN="google.com"

expiry_date=$(echo | openssl s_client -servername $DOMAIN -connect $DOMAIN:443 2>/dev/null \
| openssl x509 -noout -dates | grep notAfter | cut -d= -f2)

echo "SSL Expiry Date for $DOMAIN : $expiry_date"
